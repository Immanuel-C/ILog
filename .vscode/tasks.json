{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows Build",
            "type": "shell",
            "command": "clang ${workspaceFolder}/example.c ${workspaceFolder}/ILog.c -I${workspaceFolder}/ILog.h"
        }
    ]
}